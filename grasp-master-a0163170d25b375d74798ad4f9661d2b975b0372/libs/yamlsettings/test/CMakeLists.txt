find_package(CUNIT REQUIRED)

# FIXME: doesn't build against the current yamlsettings API.
# include_directories(${CUNIT_INCLUDE_DIRS})
# add_executable(yamlsettings_test yamlsettings_test.c)
# target_link_libraries(yamlsettings_test yamlsettings ${CUNIT_LIBRARIES})
